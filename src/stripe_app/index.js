import React from 'react'
import { SafeAreaView, ScrollView, Text } from 'react-native';

export default function StripeApp() {
    return(
        <SafeAreaView>
            <ScrollView>
                <Text>Hello from Stripe App</Text>
            </ScrollView>
        </SafeAreaView>
    )
}